suite: certificate
templates:
  - certificate.yaml
tests:
  - it: is not created when tls disabled
    asserts:
      - hasDocuments:
          count: 0

  - it: is not created when tls enabled but certManager disabled
    set:
      tls:
        enabled: true
        secretName: my-tls
    asserts:
      - hasDocuments:
          count: 0

  - it: uses custom secret name when certManager enabled
    set:
      tls:
        enabled: true
        secretName: my-tls
        certManager:
          enabled: true
          issuerRef:
            name: letsencrypt
    asserts:
      - hasDocuments:
          count: 1
      - equal:
          path: spec.secretName
          value: my-tls

  - it: is created when tls and certManager enabled without secretName
    set:
      tls:
        enabled: true
        certManager:
          enabled: true
          issuerRef:
            name: letsencrypt
            kind: ClusterIssuer
          duration: 8760h
          renewBefore: 720h
    asserts:
      - hasDocuments:
          count: 1
      - isKind:
          of: Certificate
      - equal:
          path: apiVersion
          value: cert-manager.io/v1

  - it: uses generated secret name
    set:
      tls:
        enabled: true
        certManager:
          enabled: true
          issuerRef:
            name: letsencrypt
    asserts:
      - equal:
          path: spec.secretName
          value: RELEASE-NAME-ssl-exporter-tls

  - it: sets issuerRef from values
    set:
      tls:
        enabled: true
        certManager:
          enabled: true
          issuerRef:
            name: my-issuer
            kind: Issuer
    asserts:
      - equal:
          path: spec.issuerRef.name
          value: my-issuer
      - equal:
          path: spec.issuerRef.kind
          value: Issuer

  - it: defaults dnsNames to service FQDN
    release:
      name: my-release
      namespace: monitoring
    set:
      tls:
        enabled: true
        certManager:
          enabled: true
          issuerRef:
            name: letsencrypt
    asserts:
      - contains:
          path: spec.dnsNames
          content: my-release-ssl-exporter.monitoring.svc

  - it: uses namespace override for default dnsName
    release:
      name: my-release
      namespace: default
    set:
      namespaceOverride: monitoring
      tls:
        enabled: true
        certManager:
          enabled: true
          issuerRef:
            name: letsencrypt
    asserts:
      - equal:
          path: metadata.namespace
          value: monitoring
      - contains:
          path: spec.dnsNames
          content: my-release-ssl-exporter.monitoring.svc

  - it: uses custom dnsNames when provided
    set:
      tls:
        enabled: true
        certManager:
          enabled: true
          issuerRef:
            name: letsencrypt
          dnsNames:
            - ssl-exporter.example.com
            - ssl-exporter.monitoring.svc
    asserts:
      - lengthEqual:
          path: spec.dnsNames
          count: 2
      - contains:
          path: spec.dnsNames
          content: ssl-exporter.example.com

  - it: sets duration and renewBefore
    set:
      tls:
        enabled: true
        certManager:
          enabled: true
          issuerRef:
            name: letsencrypt
          duration: 2160h
          renewBefore: 360h
    asserts:
      - equal:
          path: spec.duration
          value: 2160h
      - equal:
          path: spec.renewBefore
          value: 360h
